# v3 변경 기록

작업 지시서: `claude_code_prompt_verifier_v3.md`(D1~D9, 일반 견고성 보강)와 추가 지시(G1~G7).
프로그램 버전(`packages/common/config.py`의 `version`)은 푸시할 때마다 올린다.

## 0.6.0

| 항목 | 수정 | 시험 |
|---|---|---|
| D1 변형 인용문 | `quote_diff`: 최대 유사 구간(0.85 이상 1.0 미만)을 어절 경계까지 넓혀 삭제·추가·치환 어절을 원문과 나란히 제시(`render_quote_diff`). 정도 부사·부정·양태·조문 한정어(단서·본문·제N항)·수량이 바뀌면 CONTRADICTED(A), 그 밖의 표현 차이는 SUSPICIOUS(B). 말줄임표로 표시한 생략은 구간별 대조, 어미 1~2자 차이는 제외. 한 인용의 결함이 여럿이면 최종 판정 제목에 모두 표시. AI 교차검토 요청에 원문 대조 결과를 넣고 원문 기준 평가를 지시 | `test_v3_d1_quote_diff.py` |
| D3 의견 귀속·결론 방향 | `opinion_attribution`: 판결 전문("…의 반대의견은 다음과 같다")과 판결요지("[다수의견]", "[대법관 …의 반대의견]")를 의견 구간으로 나누고, 서면 요약이 반대의견에 뚜렷이 가까우면 MISATTRIBUTED_OPINION(반대의견에만 있는 문장을 직접 인용하면 A). 판시사항의 "…인지 여부(적극/소극/원칙적 소극)"와 반대 방향으로 단정하면 HOLDING_DIRECTION_REVERSED | `test_v3_d3_opinion.py` |
| D2 법원–사건부호 | 사건부호표 `config/legal_rules/case_codes.yaml`을 공식 별표(재판예규 제1812호, 국가법령정보센터 행정규칙 ID 2200000102523, 별표 HWP 2개)에서 `scripts/build_case_codes.py`로 생성(127개 부호). 부호→사건 유형(원문)→심급(상고·재항고·특별항고 = 대법원, 항소·항고·1심 = 하급법원, 그 밖 = 판단 안 함). 법원 종류(대법원·헌법재판소·하급법원)가 부호와 다르면 COURT_CODE_MISMATCH(A), 대법원 '누'는 1998년까지 허용·1999년 판단 안 함·2000년 이후 사용 기간 밖. 조회에 실패하면 부호가 가리키는 법원 DB로 재조회해 "법원 표시 오류(실제: 대법원 판결)"를 적는다. 인용 표기에서 법원명 뒤 조사 제거 | `test_v3_d2_case_codes.py`, `test_v3_d2_build_codes.py` |
| D5 제목 | INVALID_FORMAT 제목을 위반 유형별로: 날짜 불가능 / 법원–부호 불일치 / 연도 역전·연도 불가능 | 기존 시험 2건 기대값 갱신 |
| HWP 표 | `_extract_hwp_tables`: 표를 셀(행·열 주소) 단위로 복원 | `test_hwp_parser.py` |

경로 차이: 지시서는 `data/case_codes.yaml`을 요구했으나 `data/`는 실행 데이터 폴더(LV_DATA_DIR, git·Docker 이미지 제외)라
배포본에 들어가지 않는다. 참조 데이터는 배포되는 `config/legal_rules/`에 둔다.

사건부호표 범위: 국가법령정보센터가 제공하는 별표 파일 2개에는 민사·형사 일반 사건부호(가합·다·도 등, 별표 첫 부분)가 없다.
그 부호는 표에 넣지 않았고 판단하지 않는다(unknown).

## 0.6.1

| 항목 | 수정 | 시험 |
|---|---|---|
| D4 확인 완료 표시 | 판례: 존재·서지·인용문 일치 → VERIFIED_CITATION. 조문: 존재·주장 내용(수치 포함) 일치 → VERIFIED_PROVISION(기준일이 없으면 현행 버전 기준으로 VERIFIED, 기준일 확인은 권고로만). 사건 적용성은 APPLICABILITY_UNREVIEWED로 별도. 확인 완료 인용은 미검증 목록·미검증 비율에서 빼고 보고서에 '확인 완료 인용' 표(문서 열)로 싣는다. 집계에 verified·verified_citation·verified_provision | `test_v3_d4_verified.py`, 기존 시험 5건 기대값을 새 규칙으로 갱신 |
| D5 일관성 검사 | 불확실성 미고지(UNCERTAINTY_NOT_DISCLOSED)는 기댄 인용의 최종 판정이 NOT_FOUND·UNVERIFIED일 때만. 보고 전 `enforce_consistency`가 같은 인용과 모순되는 항목을 거른다 | `test_v3_d5_consistency.py` |
| G1·J1 헌재 결정 조회 | 조회 DB를 사건부호로 정한다(헌X → 헌재결정례 detc). '헌재' 약칭을 '헌법재판소'로 정규화. 조회 순서를 사건번호 지정 조회(nb) → 본문검색으로 바꾸고, 헌재결정례는 본문검색 3쪽까지 넘겨 본다. 응답 모양이 다르면 조회 실패(UNVERIFIED). 헌재 결정을 확인하지 못하면 NOT_FOUND가 아니라 UNVERIFIED이며, 조회 DB·방식·건수를 판정 근거에 남긴다 | `test_v3_g1_detc.py`(실존 결정 11건 모양 회귀 포함) |
| J5 | 법원 표시가 틀린 인용('대법원 … 2005헌바28 판결')도 부호가 가리키는 DB로 조회 | `test_v3_g1_detc.py` |
| 헌재 사건부호 | 「헌법재판소 사건의 접수에 관한 규칙」 제8조 제3항 사건부호표(헌가·헌나·헌다·헌라·헌마·헌바·헌사·헌아)를 공식 원문으로 확인해 사건부호표에 등록 | — |

공식 원문 수집(CI)에서 확인한 사실: 헌재결정례 API는 사건번호 지정 조회(nb)와 기본 검색이 실존 결정 11건(2004헌마554,
2016헌나1, 2004헌나1, 2017헌바127, 2008헌가23, 2011헌바379, 2009헌바17, 2013헌다1, 2015헌마236, 96헌가2, 89헌마82)을
모두 정확히 돌려주었고, 본문검색(search=2)은 다른 결정 수십~수천 건을 돌려주었다.

## 0.6.2

| 항목 | 수정 | 시험 |
|---|---|---|
| D3 보강 | 판시 방향 검사를 절 단위로(쉼표·연결어미로 나눔), 긍정·부정은 절 끝 서술어로만 판단("거치지 않은 채 … 징계사유가 된다"는 긍정). 쟁점 대응은 글자 2-gram과 어간(어절 앞 두 글자) 겹침으로 보고, 두 번째 쟁점보다 뚜렷이 가까울 때만 대응시킨다. 대응이 약하면 SUSPICIOUS(C, 사람 확인). 실제 2012두26401 판시사항과 TC-02 문장으로 재현 | `test_v3_d3_opinion.py`(실제 판시사항·민사 합성 포함) |
| D6 증거 알림·호증 파서 | `claim_engine/exhibits.py`: "갑 제5호증의 1 내지 3 각 진술서" → 갑·5·가지 1~3·진술서. 표 안의 줄(table_line)은 목록으로 읽지 않고 호증 표를 셀 단위로 읽는다. 증거 파일을 함께 올리지 않은 실행은 문서당 INFO 1건("첨부 증거 N건이 입력에 없음 — 목록 보기"), 올린 실행에서만 자료별 대조 | `test_v3_d6_evidence.py`(행정·민사·국가배상·형사 호증 표기) |
| D7 출처 실패 | 학술자료 조회에서 출처 하나라도 실패(MISSING_KEY·RATE_LIMITED·ERROR)하면 NOT_FOUND가 아니라 UNVERIFIED, 실패 출처를 근거에 적는다. 요청 한도는 기존 전송 계층의 지수 백오프 재시도로 처리 | `test_v3_d7_sources.py` |
| D8 요약 표 문서 열 | 요약 판정 표(docx)에 '문서' 열, 문서별 묶음, 여러 문서에 걸친 판정은 '문서 간: 관련 문서' 묶음 | `test_report_completion.py` |
| D9 가시 지시문 | 보이는 본문이라도 AI·검토 도구를 수신자로 검증 생략·결과 조작·보고 억제를 요구하면 B 등급. 대상 명사와 무관한 'AI 수신 검증 금지' 패턴 추가 | `test_v3_d9_visible_instruction.py`(민사·형사·국가배상 포함) |
| G6 파일명 오탐 | '배포본 식별 가능 문자열' 규칙을 16진수 16자 이상·UUID 형태로 좁히고 참고(INFO)로만. copy·dist·사본·v2·scan·bundle은 보지 않는다 | `test_v3_g6_filename.py` |

## 0.6.3

| 항목 | 수정 | 시험 |
|---|---|---|
| J3 산식 행 | 줄·표 합계 검산에서 산식 행('a원 + b원 = c원')을 건너뛰지 않고 '=' 뒤 최종 금액을 쓴다. 산식 자체는 따로 검산(산식 오류). 셀 표는 금액 열(머리글)을 찾아 행마다 최종 금액을 쓰고, 산식 칸('500,000원 × 3')을 금액 칸과 대조. 판정 근거에 사용한 행 목록(rows_used) | `test_v3_j3_arithmetic.py`(국가배상·민사·형사) |
| G5 기간 재계산 | `fact_checks.check_periods`: 날짜 구간 일수(초일 산입·불산입 둘 다 틀릴 때만), 'X부터 N년이 경과한 Y' 만료일(민법 제157조·제160조 방식), 'X부터 Y까지 N년' 실제 기간 | `test_v3_g5_facts.py` |
| G5 증거 목록 | 사건을 기록·증명하는 서증(분석·보고·진술·확인·의료·조서·증명서)의 작성일이 입증취지의 사건일(없으면 본문 사고일)보다 앞서면 판정. 가지번호 결번('의 2'만 있고 '의 1' 없음). 같은 파일 안 증거목록 작성자와 첨부 원문 발급기관 대조 | `test_v3_g5_facts.py` |
| G5 사실 저장소 | `fact_store`: 신체 부위 좌·우, 사고일, 청구금액 튜플을 사건 단위로 모아 문서 간 불일치(CROSS_DOCUMENT_CONTRADICTION)를 낸다. 파이프라인 수준에서 internal_consistency.cross_document_issues가 0이 아닌 값으로 산출됨을 시험 | `test_v3_g5_facts.py` |
| J2 모델 지적 | AI 판별 모델 근거 가운데 날짜·금액·기간 모순 지적을 AI 작성 근거에서 빼 MODEL_FACT_REMARK로 분리. 결정론 재계산에 같은 종류 판정이 있으면 그 판정(CONTRADICTED)의 근거로 붙이고, 없으면 사람 확인 항목으로 남긴다 | `test_v3_j2_model_remarks.py` |

| 단계 | 본(오프라인) | 홀드아웃(오프라인) | 본(실연동) | 홀드아웃(실연동) |
|---|---|---|---|---|
| v2 최종(50b7099) | 81.7 | 72.2 | 98.2 | 87.0 |
| 0.6.0 | 85.3 (FP-TRAP 0) | 75.0 (FP-TRAP 0) | 98.2 (FP-TRAP 0) | 87.0 (FP-TRAP 0) |
| 0.6.1 | 85.3 (FP-TRAP 0) | 75.0 (FP-TRAP 0) | 98.2 (FP-TRAP 0) | 87.0 (FP-TRAP 0) |
| 0.6.2 | 85.3 (FP-TRAP 0) | 75.0 (FP-TRAP 0) | 98.2 (FP-TRAP 0) | 87.0 (FP-TRAP 0) |
| 0.6.3 | 85.3 (FP-TRAP 0) | 75.0 (FP-TRAP 0) | 98.2 (FP-TRAP 0) | 87.0 (FP-TRAP 0) |
| 0.6.6 | 85.3 (FP-TRAP 0) | 81.9 (FP-TRAP 0) | 98.2 (FP-TRAP 0) | 94.4 (FP-TRAP 0) |
| 0.6.8 | 85.3 (FP-TRAP 0) | 81.9 (FP-TRAP 0) | 98.2 (FP-TRAP 0) | 94.4 (FP-TRAP 0) |

## 0.6.4

| 항목 | 수정 | 시험 |
|---|---|---|
| 인용 오류 대조표 칸 정리 | '판례 인용 오류·미확인 검토' 표(화면·Word·PDF)에서 '주장 평가' 칸을 없애고 평가를 '인용 오류·미확인 근거' 칸 머리에 함께 싣는다(화면은 배지, 보고서는 '[평가] …'). 남는 폭은 '법리적 타당성 검토 및 반박 근거' 칸에 준다(화면 45%, Word 2.7in, PDF 215pt). PDF의 법리 검토 발췌 길이도 넓어진 칸에 맞춰 늘림 | `test_frontend_model_opinions.py`, `test_report_completion.py` |

## 0.6.5

| 항목 | 수정 | 시험 |
|---|---|---|
| 휴지통 영구 삭제 | 휴지통 창에 '영구 삭제' 단추(한 번 더 확인). `DELETE /api/projects/{id}/purge`는 휴지통에 있는 프로젝트만, 프로젝트 관리자만 실행한다. 스키마의 외래키를 따라 프로젝트에 속한 행을 자식부터 지우고, 원본·파생물·평문 사본·가명 처리 보관소를 지운다. 감사기록은 남기고 영구 삭제 사실(지운 건수)을 추가한다. PostgreSQL(외래키·감사기록 변경 금지 트리거)에서 21개 테이블 삭제 확인 | `test_project_lifecycle.py`, `test_storage_encryption.py`, `test_frontend_project_tools.py` |
| HTTP 500 추적 | 처리하지 못한 오류를 문의 번호가 담긴 JSON으로 돌려주고, 로그에 요청 경로·예외 종류·호출 위치를 남긴다(예외 메시지는 사건 정보가 섞일 수 있어 기록하지 않음) | `test_api.py` |
| 진행률 스트림 커넥션 점유 | SSE 진행률 스트림이 요청 세션의 DB 커넥션을 스트림 내내(최대 5분) 붙잡았다. 커넥션 풀 5개(운영 설정)에서 스트림 5개를 열면 다른 요청이 22초 기다리고 일부는 풀 대기 초과(TimeoutError, HTTP 500)로 실패함을 PostgreSQL에서 재현했다. 권한 확인 직후 세션을 돌려준다(수정 후 스트림 8개에서 다른 요청 0.02초) | `test_progress_stream_pool.py` |
| 오류 가림 | 접근 미들웨어가 엔드포인트 안의 ValueError(데이터 검증 오류 등)까지 '인증 설정 오류(503)'로 바꿨다. 인증 단계의 오류만 503으로 둔다 | `test_api.py` |

## 0.6.6

| 항목 | 수정 | 시험 |
|---|---|---|
| G4 법리 주장 검토 재설계 | `legal_engine/claim_review.py`: 문장을 주장 유형(위헌 주장·전칭 일반화·법률상 근거 없는 청구 유형·조문 요건 불일치·출처 불명 기준·소송요건 배제)으로 먼저 나누고, 유형별 근거를 조회해 판단한다. 위헌 주장은 대상이 헌법 조항이면 형식상 심판 대상 아님(헌법재판소법 제41조·제68조), 법률 조항을 단정적으로 위헌이라 하면 헌재 결정 이력(`LawGoKrAdapter.constitutional_history`: 조항 검색 → 사건명 조항 일치 → 주문에서 결과 판독)과 대조해 합헌이면 모순(B), 각하면 사람 확인(C), 조회 불가면 확인 불가(UNVERIFIED). 전칭 명제는 근거가 없으면 `UNSUPPORTED_GENERALIZATION`(새 판정 유형). 앞 문장의 인용은 이 문장이 이어받을 때('따라서'·'이런' 등)만 근거로 본다. 헌법의 일반 기본권 조항만 든 소송요건 배제는 예외 근거로 보지 않는다. 조문 요건 대조는 조회한 공식 조문 원문의 주체와 적용 대상을 비교하고 비교 대상 조문을 근거에 적는다 | `test_v3_g4_claims.py` |
| G4 합성 시험 | 무리한 주장 30·타당한 주장 30(행정·민사·국가배상·형사·노동). 정밀도 1.000·재현율 1.000(조정에 쓴 시험셋). 보류 시험셋: 1차 20건 첫 측정 정밀도 1.000·재현율 0.600 → 일반 원리 보강 후 2차 20건(조정 뒤 한 번만 측정) 정밀도 1.000·재현율 0.900, 측정에서 드러난 법령명 추출 결함 수정 후 1.000 | `g4_claims.json`, `g4_claims_holdout.json` |
| v2 규칙 오탐 | ADMIN.DEADLINE_EXCEPTION이 '무효확인소송에는 제소기간 제한이 적용되지 않는다'(정확한 법리)까지 판정했다. 규칙표에 `unless`(무효확인·정당한 사유)를 두어 법이 정한 예외를 든 문장은 판정하지 않는다 | `test_v3_g4_claims.py` |
| 공식 원문 수집 | G4 근거 조문(헌법 제111조, 헌법재판소법 제41·45·68조, 민법 제162·166·393·394·750·751·763·764·766조, 형사소송법 제246조, 행정소송법 제12·18·19조, 국가배상법 제8·9조, 제조물 책임법 제3조)과 조항별 헌재 결정 검색·결정 전문 키 점검을 수집 목록에 추가 | CI '공식 원문 수집' |

평가(오프라인): 본 85.3(변화 없음, FP-TRAP 0), 홀드아웃 75.0 → 81.9(FP-TRAP 0). 근거 조문 원문은 공식 원문 수집 결과를 받아 규칙표에 옮긴 뒤 판정 근거에 싣는다(그 전에는 조문 이름만 싣고 '원문 미수록'을 밝힌다).

## 0.6.7

| 항목 | 수정 | 시험 |
|---|---|---|
| G2 한국어 OCR 품질 | 쪽마다 OCR 평균 신뢰도와 한글 비율(한글/(한글+로마자))을 잰다(`ocr.page_quality`). 한국어 모델(kor)로 읽었는데 한글 비율이 0.5 미만(글자 20자 이상)이거나 평균 신뢰도가 0.70 미만이면 그 쪽을 `OCR_LOW_QUALITY`로 표시하고, `OCR_LOW_QUALITY` 판정(UNVERIFIED)과 미검증 항목을 남겨 배포 게이트가 통과로 결론 내리지 않게 한다. 읽은 글자로 날짜·사건번호 검사는 계속한다. 이미지 문서도 같은 기준. 기준값은 `LV_OCR_QUALITY_MIN_CONFIDENCE`·`LV_OCR_MIN_HANGUL_RATIO` | `test_v3_g2_ocr_quality.py` |
| G2 OCR 띄어 읽기 | 한글 OCR이 음절마다 띄어 읽은 부분('헌 법 재판소', '2020 헌 마 1127')을 붙인다. 한 글자 한글 어절이 둘 이상 이어질 때만 합친다. 법원명 '헌법 재판소'와 사건번호 '헌 마'의 띄어쓰기를 허용 | 같음(나눔고딕으로 만든 한국어 스캔 합성 문서를 실제 엔진으로 읽어 날짜 2019-02-30·2021-11-31, 사건번호 2018두47215·2020헌마1127 추출 확인) |
| G3 항·호 결합 | '같은 조 제N항', '동조 제N항', '위 조항', 조 없이 쓴 '제N항'을 앞 인용(300자 안)의 조로 풀어 별도 인용으로 둔다. 뒤따르는 수치는 가장 가까운 항 인용에 결합된다('제766조 제1항은 3년, 같은 조 제2항은 10년' → 10년은 제2항과 대조). '같은 조건'·'위조한'·'동조하였다'는 인용이 아니다. 수치 불일치 판정의 제목·근거에 비교 대상 조문을 푼 이름으로 적는다('민법 제766조 제2항('같은 조 제2항')') | `test_v3_g3_paragraph_binding.py`(민사·행정·국가배상·노동·형사) |
| G4 근거 원문 | 공식 원문 수집(CI) 결과에서 대한민국헌법 제111조, 민법 제162·393·394·763·764·766조, 형사소송법 제246조, 행정소송법 제12·18조, 국가배상법 제8·9조, 제조물 책임법 제3조, 헌법재판소 1991. 4. 1. 89헌마160 결정(결정요지)을 규칙표 근거로 옮겼다. 헌재결정례 상세 응답에 '주문' 키가 없고 전문에 주문이 있음을 확인해 결과를 전문의 주문에서 읽는다. 헌법재판소법은 같은 이름의 법령이 둘 이상이라 수집되지 않아(식별 모호) 이름만 싣는다 | `test_v3_g4_claims.py` |

평가(오프라인): 본 85.3, 홀드아웃 81.9(FP-TRAP 0, 변화 없음).

## 0.6.8

| 항목 | 수정 | 시험 |
|---|---|---|
| J4 렌더링 가시성 | 흰색 일치 규칙만으로는 놓치던 숨김 글자를 잡는다. ① 채움 투명도(ExtGState `ca`)가 0.1 이하인 글자 → `TRANSPARENT_FILL`(q/Q·gs 상태를 따라가 복원 후 글자는 보이는 글자로 둔다) ② 글자 뒤에 그린 이미지가 글자를 덮으면 → `COVERED_BY_IMAGE`(그린 순서로 판단) ③ 흰색이 아닌 글자·색 상자 위 글자는 쪽을 렌더링해 글자 상자의 배경색을 읽고 WCAG 대비비가 1.5 미만이면 `LOW_CONTRAST(x:1)`(흰 바탕 흰 글자는 `WHITE_ON_WHITE` 유지). 어두운 띠 위 흰 제목처럼 대비가 충분한 글자는 숨김에서 뺀다(오탐 제거). 숨김 경로 이름('투명 글자', '이미지로 덮은 글자', '배경과 대비가 거의 없는 글자')을 인젝션 보고에 싣는다 | `test_v3_j4_visibility.py`(행정·민사·형사·국가배상·노동) |
| 영구 삭제가 되지 않던 문제 | 보고서 생성 중 서버가 다시 시작되거나 작업 스레드가 오류로 사라지면 보고서 작업이 RUNNING으로 남고, 화면이 그 작업을 조회하지 않는 한 닫히지 않았다. 영구 삭제는 이 작업을 '생성 중'으로 보아 계속 409로 거부했다(PostgreSQL에서 재현). 진행 기록이 2분간 없는 작업은 중단으로 닫고 삭제한다(`reports.expire_stale_jobs`). 살아 있는 작업은 그대로 409 | `test_project_lifecycle.py` |
| 대화상자 뒤에 가려지던 알림 | 알림(`#toast`)이 일반 요소라 모달 대화상자(휴지통 창)가 열려 있으면 배경막 뒤에 흐리게 가려졌다. 그래서 거부 사유가 보이지 않고 목록만 그대로 남아 '눌러도 사라지지 않는' 것으로 보였다. 알림을 popover(최상위 층)로 띄우고, 영구 삭제 실패 사유는 해당 항목 바로 아래에도 적는다 | `test_frontend_project_tools.py` |

평가(오프라인): 본 85.3, 홀드아웃 81.9(FP-TRAP 0, 미매칭 판정 변화 없음. J4 수정 중 한때 이미지 가림 판정이 빠져 80.1로 떨어진 것을 바로잡고 확인).
